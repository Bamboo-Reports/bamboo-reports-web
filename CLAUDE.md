# Repository Instructions

- Never run build commands such as `npm run build`, `vite build`, `npm run preview`, or similar production build/preview commands.
- Never run development server commands such as `npm run dev`, `npm run dev:netlify`, `vite`, or similar long-running local server commands.
