# Repository Instructions

- Do not run development server commands such as `npm run dev`, `npm run dev:netlify`, `vite`, or similar long-running local server commands unless the user explicitly asks for them.
- Do not run build commands such as `npm run build`, `vite build`, `npm run preview`, or similar production build/preview commands unless the user explicitly asks for them.
